

// export function setStateAsync(state, that) {
//   return new Promise((resolve) => {
//     that.setState(state, resolve);
//   });
// }

// //  call await setStateAsync(<new state>, this)

// function asyncSetState = instance => newState => new Promise(resolve => instance.setState(newState, resolve));